name: dv_network
description: Network and graph visualization components.
repository: https://github.com/gsmlg-dev/data_visualization/tree/main/packages/dv_network
homepage: https://github.com/gsmlg-dev/data_visualization
version: 1.0.0
resolution: workspace

environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dv_point:
  dv_shape:
  dv_glyph:
  dv_drag:
  dv_zoom:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
