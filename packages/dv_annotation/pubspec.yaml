name: dv_annotation
description: Annotation overlays for charts - reference lines, areas, labels.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dv_scale:
  dv_text:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
