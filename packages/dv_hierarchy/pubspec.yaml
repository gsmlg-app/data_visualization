name: dv_hierarchy
description: Hierarchical data structures and layout algorithms.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  dv_vendor:

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
